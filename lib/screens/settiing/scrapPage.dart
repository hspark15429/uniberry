

import 'package:flutter/material.dart';

class ScrapPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('스크랩'),
      ),
      body: Center(
        child: Text('스크랩 페이지입니다.'),
      ),
    );
  }
}
